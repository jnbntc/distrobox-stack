FROM docker.io/library/debian:bookworm-slim

RUN apt-get update && \
    apt-get install -y --no-install-recommends \
    python3 python3-venv git curl build-essential make && \
    apt-get clean && \
    rm -rf /var/lib/apt/lists/*
